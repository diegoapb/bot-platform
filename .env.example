# ===========================================================================
# bot-plataform — plantilla de variables (raíz).
# Copia a .env y rellena. El .env real NO se versiona.
# Cada app tiene además su propio .env.example (apps/backend, apps/frontend).
# ===========================================================================

NODE_ENV=development
