# ===========================================================================
# bot-plataform / frontend (Vite) — variables expuestas al browser (VITE_*).
# ===========================================================================

# URL pública del backend Hono.
VITE_API_URL=https://api.bots.diegop.com

# Clerk (clave pública, segura para el browser).
VITE_CLERK_PUBLISHABLE_KEY=pk_test_CHANGE_ME
